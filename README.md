# Invoice App

Simple Flask app to generate invoices with multiple products.

## How to Run

1. Install dependencies: `pip install flask`
2. Run the app: `python app.py`
3. Open browser: http://127.0.0.1:5000/
